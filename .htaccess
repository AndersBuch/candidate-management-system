Options -MultiViews

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /app/
  # Serve real files/dirs as-is
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Everything else -> index.html (note: no leading slash)
  RewriteRule ^ index.html [L]
</IfModule>
